namespace CoreNodes;

/// <summary>
/// Temporary so that we can compile... but i think TargetModes will be a class that holds its own logic in the future.
/// </summary>
public enum TargetMode
{
	Random,
	All,
	Target,
	None,
}
